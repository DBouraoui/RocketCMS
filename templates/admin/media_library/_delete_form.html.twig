<form method="post" action="{{ path('app_admin_media_library_delete', {'id': media_library.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media_library.id) }}">
    <button class="btn btn-error">Supprimer</button>
</form>
