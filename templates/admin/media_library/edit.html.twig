{% extends 'admin/base.html.twig' %}

{% block title %}Modifier un élément — Médiathèque{% endblock %}

{% block body %}
    {% include 'admin/components/Header.html.twig' with {
        title: 'Modifier un élément de la médiathèque',
        subtitle: 'Mettez à jour les informations ou remplacez l’image existante.'
    } %}

    <div class="p-8 mb-8 bg-white shadow rounded-lg">
        {{ include('admin/media_library/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="flex justify-between items-center">
        <a href="{{ path('app_admin_media_library_index') }}" class="btn btn-outline">
            ← Retour à la liste
        </a>

        {{ include('admin/media_library/_delete_form.html.twig') }}
    </div>
{% endblock %}
