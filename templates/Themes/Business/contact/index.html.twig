{% extends './Themes/Business/base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-white text-neutral-900 p-6">
        <div class="w-full max-w-2xl text-center">
            <h1 class="text-3xl font-extrabold tracking-tight mb-3">{{ contact.title|default('Une question une demande ?')}}</h1>
            <p class="text-neutral-500 mb-8">
                {{ contact.subtitle|default('Contactez nous !') }}
            </p>

            {% include 'Themes/Business/contact/_form.html.twig' %}

        </div>
    </div>
{% endblock %}
