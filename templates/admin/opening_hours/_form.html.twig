{{ form_start(form, { attr: { class: 'space-y-6' } }) }}
    {{ form_widget(form) }}

<div class="mt-4">
    <button type="submit"
            class="px-5 py-2.5 bg-neutral-900 text-white rounded-lg hover:bg-neutral-800 transition font-medium">
        {{ button_label|default('Enregistrer') }}
    </button>
</div>
{{ form_end(form) }}
