{% extends './Themes/Business/base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-gray-50 p-6">
        <div class="w-full max-w-md">
            <!-- Card Container -->
            <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
                <!-- Header Section -->
                <div class="px-6 py-8 sm:px-8 sm:py-10 text-center">
                    <h1 class="text-3xl font-bold text-black">Bienvenue</h1>
                    <p class="text-gray-600 mt-2 text-sm sm:text-base">Connectez-vous à votre compte</p>
                </div>

                <!-- Form Section -->
                <div class="px-6 py-8 sm:px-8 sm:py-10">
                    {% if error %}
                        <div class="alert alert-error mb-6">
                            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                            <span class="text-sm">{{ error.messageKey|trans(error.messageData, 'security') }}</span>
                        </div>
                    {% endif %}

                    {% if app.user %}
                        <div class="alert alert-soft mb-6">
                        <span class="text-sm">
                            Vous êtes connecté en tant que <strong>{{ app.user.userIdentifier }}</strong>.
                            <a href="{{ path('app_logout') }}" class="link link-hover font-medium">Se déconnecter</a>
                        </span>
                        </div>
                        <div class="alert alert-soft mb-6">
                            <a href="{{ path('app_admin_dashboard_index') }}" class="link link-hover font-medium">Mon tableau de bord</a>
                        </div>
                    {% endif %}

                    <form method="post" class="space-y-5 sm:space-y-6">
                        <!-- Email Field -->
                        <div class="form-control w-full">
                            <label for="username" class="label">
                                <span class="label-text font-medium text-black">Adresse email</span>
                            </label>
                            <input
                                type="email"
                                value="{{ last_username }}"
                                name="_username"
                                id="username"
                                autocomplete="email"
                                required
                                autofocus
                                class="input input-bordered w-full rounded-md focus:ring-2 focus:ring-black"
                                placeholder="vous@exemple.com"
                            >
                        </div>

                        <!-- Password Field -->
                        <div class="form-control w-full">
                            <label for="password" class="label">
                                <span class="label-text font-medium text-black">Mot de passe</span>
                            </label>
                            <input
                                type="password"
                                name="_password"
                                id="password"
                                autocomplete="current-password"
                                required
                                class="input input-bordered w-full rounded-md focus:ring-2 focus:ring-black"
                                placeholder="••••••••"
                            >
                        </div>

                        <!-- Remember Me Checkbox -->
                        <div class="form-control">
                            <label class="label cursor-pointer justify-start gap-3">
                                <input
                                    type="checkbox"
                                    name="_remember_me"
                                    id="_remember_me"
                                    class="checkbox checkbox-sm checkbox-black"
                                >
                                <span class="label-text text-black">Se souvenir de moi</span>
                            </label>
                        </div>

                        <!-- CSRF Token -->
                        <input
                            type="hidden"
                            name="_csrf_token"
                            value="{{ csrf_token('authenticate') }}"
                        >

                        <!-- Submit Button -->
                        <button
                            type="submit"
                            class="btn w-full bg-black text-white font-semibold py-3 rounded-md hover:bg-gray-800 transition"
                        >
                            Se connecter
                        </button>
                    </form>
                </div>
            </div>

            <!-- Footer Text -->
            <p class="text-center text-gray-500 text-xs sm:text-sm mt-6 sm:mt-8">
                © 2025 - Tous droits réservés
            </p>
        </div>
    </div>
{% endblock %}
